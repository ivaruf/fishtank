{
  "name": "fishtank",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "format:check": "prettier --check client shared tools tests README.md docs",
    "build:static": "node tools/build-static.mjs",
    "serve": "python3 -m http.server 3000"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "babylonjs": "^8.0.0",
    "babylonjs-loaders": "^8.0.0"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
